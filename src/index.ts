/**
 * @description
 * this is to find a number odd or even
 * @param {number} n
 * @returns {boolean}
 * @example
 * isOdd(1) // true
 * isOdd(2) // false
 * */
export const isOdd = (n: number): boolean => n % 2 === 1;
export * from "./Button";
export * from "./components";
